[package]
name = "locnative"
version = "1.0.0"
edition = "2021"

[lib]
name = "locnative"
crate-type = ["cdylib"]

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
